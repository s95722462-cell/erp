# ⚙️ 테이블 항목 순서 변경 기능 설계 (iERP v2)

이 문서는 iERP v2의 '판매용' 소프트웨어 가치를 높이기 위한 **사용자 정의 테이블 컬럼 순서 변경** 기능에 대한 설계 내용을 담고 있습니다.

---

## 1. 개요
현재 구현된 '항목 선택(보이기/숨기기)' 기능을 확장하여, 사용자가 표의 컬럼 순서를 원하는 대로 배치할 수 있게 함으로써 사용자 경험(UX)과 업무 효율을 극대화합니다.

## 2. 주요 기능
- **직관적인 순서 조정**: 마우스 드래그 앤 드롭(Drag & Drop) 또는 위/아래 버튼을 통한 순서 변경.
- **실시간 반영**: 설정을 변경하는 즉시 테이블의 헤더와 데이터 바디가 재배치됨.
- **영구 저장**: 사용자가 설정한 순서는 브라우저(`localStorage`) 또는 서버(`Firebase`)에 저장되어 다음 접속 시에도 유지됨.

## 3. 기술적 구현 방안

### 3.1 데이터 구조 확장
기존의 `activeCols` 배열의 요소 순서를 사용자가 변경한 순서 그대로 유지합니다.
```javascript
// 예시: 날짜와 거래처 순서를 바꾼 경우
activeCols.sales = ['buyer', 'date', 'item', ...];
```

### 3.2 UI 구현 (설정 모달)
- **방식 1 (추천)**: `Sortable.js`와 같은 경량 라이브러리를 사용하여 항목 리스트를 마우스로 끌어서 이동.
- **방식 2**: 각 항목 오른쪽에 `↑`, `↓` 버튼을 배치하여 클릭 시 배열 내 인덱스 위치를 변경.

### 3.3 렌더링 로직
이미 구현된 `renderDynamicTable` 함수가 `activeCols`의 순서를 기반으로 루프를 돌기 때문에, 배열의 순서만 바꿔주면 추가적인 렌더링 로직 수정 없이 완벽하게 작동합니다.

## 4. 기대 효과
1. **커스텀 최적화**: 유저별로 가장 중요하게 생각하는 데이터를 표의 앞쪽(왼쪽)에 배치하여 시독성 향상.
2. **상품 가치 상승**: 엑셀보다 편리한 웹 기반 UI 제공으로 월 구독형 서비스로서의 경쟁력 확보.

---
**작성일**: 2026-06-02
**작성자**: 탱고 (iERP 개발 파트너)
