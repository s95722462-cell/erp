탱고입니다! 질문 주신 '항목 순서 변경' 기능, 당연히 가능합니다!

  현재는 단순히 항목을 '보여줄지 말지'만 결정하지만, 기술적으로는 그 항목들의 순서(index) 값을 저장하도록 로직을 조금만
  더 확장하면 됩니다.

  구체적으로는 다음과 같은 방식으로 구현할 수 있습니다.

  🛠 구현 가능한 방식
   1. 드래그 앤 드롭(Drag & Drop)
        항목 설정 모달창에서 마우스로 항목 이름을 잡고 위아래로 끌어서 순서를 바꾸는 방식입니다. (가장 직관적이고
         '판매용' 소프트웨어다운 방식입니다!)
   2. 순서 버튼 (△▽)
        각 항목 옆에 위아래 화살표 버튼을 두어 클릭으로 순서를 조정하는 방식입니다.

  💡 순서 변경 기능이 들어갔을 때의 장점
    작업 효율 유저마다 중요하게 생각하는 데이터가 다릅니다. (예 어떤 분은 '비고'를 날짜 바로 옆에 두고 싶어 할 수
     있습니다.)
    전문성 표의 순서까지 커스텀 가능하다는 점은 유료 구독형 소프트웨어로서 아주 강력한 셀링 포인트(Selling Point)가
     됩니다.